# Native JSON Schema structured output (`output_config.format`) for the Batch
# implementation.  `anthropic` 1.0.0 requires Python >=3.10.
anthropic==1.0.0
pyzotero
pymupdf
pdfplumber
pytesseract
pillow          # imported by the OCR fallback in src/pdf_extract.py
rapidfuzz
openpyxl        # reads the GroundTruth*.xlsx label files
pydantic
tenacity
tqdm
python-dotenv
pandas
